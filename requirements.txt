# ── Core HTTP & Async ─────────────────────────────────────────────────────────
httpx==0.27.0                # Async HTTP client; used by all API clients
anyio==4.3.0                 # Async primitives (httpx dependency)

# ── CLI & Terminal UI ─────────────────────────────────────────────────────────
typer==0.26.7                # Ergonomic CLI builder (wraps Click)
rich==15.0.0                 # Beautiful terminal output, progress bars, tables

# ── Data Validation & Settings ────────────────────────────────────────────────
pydantic==2.7.1              # Data models with runtime validation
pydantic-settings==2.2.1     # .env loading via Pydantic BaseSettings
python-dotenv==1.0.1         # .env file support (also used standalone)

# ── Retry Logic ───────────────────────────────────────────────────────────────
tenacity==8.3.0              # Retry with exponential backoff, jitter, predicates

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0                # Test runner
pytest-asyncio==0.23.6       # asyncio support for pytest
pytest-mock==3.14.0          # Mock fixtures (wraps unittest.mock)
coverage==7.5.0              # Code coverage reporting

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dateutil==2.9.0       # Date parsing utilities
